# ********RoostGPT********
"""
Test generated by RoostGPT for test deepseek using AI Type deepseek_ai and AI Model deepseek-chat

Test generated by RoostGPT for test deepseek using AI Type deepseek_ai and AI Model deepseek-chat

ROOST_METHOD_HASH=__init___edbb34f08d
ROOST_METHOD_SIG_HASH=__init___bba406a912


### Test Scenarios for `__init__` Method

#### Scenario 1: Initialization with a `.txt` File
**Details:**
  - **TestName:** test_init_with_txt_file
  - **Description:** This test verifies that the `__init__` method correctly initializes the object when provided with a `.txt` file. It checks if the `file_extension` is set to `.txt` and if the output directory is correctly created or truncated.

**Execution:**
  - **Arrange:** 
    - Create a temporary `.txt` file for testing.
    - Ensure the `file_split` directory does not exist initially.
  - **Act:** 
    - Instantiate the `Split_Files` class with the `.txt` file and a valid `split_number`.
  - **Assert:** 
    - Verify that `self.file_extension` is set to `.txt`.
    - Check that the `file_split` directory exists and is empty.

**Validation:**
  - This test ensures that the initialization logic correctly handles `.txt` files, which is crucial for the subsequent file-splitting operations. It also validates the directory management logic, ensuring that the output directory is properly prepared for new operations.

#### Scenario 2: Initialization with a `.csv` File
**Details:**
  - **TestName:** test_init_with_csv_file
  - **Description:** This test verifies that the `__init__` method correctly initializes the object when provided with a `.csv` file. It checks if the `file_extension` is set to `.csv` and if the output directory is correctly created or truncated.

**Execution:**
  - **Arrange:** 
    - Create a temporary `.csv` file for testing.
    - Ensure the `file_split` directory does not exist initially.
  - **Act:** 
    - Instantiate the `Split_Files` class with the `.csv` file and a valid `split_number`.
  - **Assert:** 
    - Verify that `self.file_extension` is set to `.csv`.
    - Check that the `file_split` directory exists and is empty.

**Validation:**
  - This test ensures that the initialization logic correctly handles `.csv` files, which is essential for the subsequent file-splitting operations. It also validates the directory management logic, ensuring that the output directory is properly prepared for new operations.

#### Scenario 3: Initialization with an Existing Output Directory
**Details:**
  - **TestName:** test_init_with_existing_directory
  - **Description:** This test verifies that the `__init__` method correctly handles the scenario where the `file_split` directory already exists. It checks if the existing directory is truncated and recreated.

**Execution:**
  - **Arrange:** 
    - Create a temporary `.txt` file for testing.
    - Manually create the `file_split` directory and add some dummy files to it.
  - **Act:** 
    - Instantiate the `Split_Files` class with the `.txt` file and a valid `split_number`.
  - **Assert:** 
    - Verify that the `file_split` directory exists and is empty after initialization.

**Validation:**
  - This test ensures that the initialization logic correctly manages the output directory, especially in cases where it already exists. This is important to prevent any residual files from previous operations from interfering with the current process.

#### Scenario 4: Initialization with an Invalid File Extension
**Details:**
  - **TestName:** test_init_with_invalid_file_extension
  - **Description:** This test verifies that the `__init__` method correctly handles files with unsupported extensions by defaulting to `.csv`.

**Execution:**
  - **Arrange:** 
    - Create a temporary file with an unsupported extension (e.g., `.docx`).
  - **Act:** 
    - Instantiate the `Split_Files` class with the unsupported file and a valid `split_number`.
  - **Assert:** 
    - Verify that `self.file_extension` is set to `.csv`.

**Validation:**
  - This test ensures that the initialization logic gracefully handles files with unsupported extensions by defaulting to `.csv`. This is important for robustness, ensuring that the function does not fail unexpectedly when encountering unexpected file types.

#### Scenario 5: Initialization with a Non-Integer Split Number
**Details:**
  - **TestName:** test_init_with_non_integer_split_number
  - **Description:** This test verifies that the `__init__` method correctly handles non-integer `split_number` values by converting them to integers.

**Execution:**
  - **Arrange:** 
    - Create a temporary `.txt` file for testing.
  - **Act:** 
    - Instantiate the `Split_Files` class with the `.txt` file and a non-integer `split_number` (e.g., a string that can be converted to an integer).
  - **Assert:** 
    - Verify that `self.split` is an integer.

**Validation:**
  - This test ensures that the initialization logic correctly handles non-integer `split_number` values by converting them to integers. This is important for flexibility, allowing the function to accept a wider range of input formats.

#### Scenario 6: Initialization with a Negative Split Number
**Details:**
  - **TestName:** test_init_with_negative_split_number
  - **Description:** This test verifies that the `__init__` method correctly handles negative `split_number` values, ensuring that the split number is always positive.

**Execution:**
  - **Arrange:** 
    - Create a temporary `.txt` file for testing.
  - **Act:** 
    - Instantiate the `Split_Files` class with the `.txt` file and a negative `split_number`.
  - **Assert:** 
    - Verify that `self.split` is a positive integer.

**Validation:**
  - This test ensures that the initialization logic correctly handles negative `split_number` values by converting them to positive integers. This is important for ensuring that the file-splitting operation proceeds correctly, as a negative split number would not make sense in this context.

#### Scenario 7: Initialization with a Zero Split Number
**Details:**
  - **TestName:** test_init_with_zero_split_number
  - **Description:** This test verifies that the `__init__` method correctly handles a `split_number` of zero, ensuring that the split number is set to a valid positive integer.

**Execution:**
  - **Arrange:** 
    - Create a temporary `.txt` file for testing.
  - **Act:** 
    - Instantiate the `Split_Files` class with the `.txt` file and a `split_number` of zero.
  - **Assert:** 
    - Verify that `self.split` is a positive integer.

**Validation:**
  - This test ensures that the initialization logic correctly handles a `split_number` of zero by converting it to a valid positive integer. This is important for ensuring that the file-splitting operation proceeds correctly, as a split number of zero would not make sense in this context.

#### Scenario 8: Initialization with a Large Split Number
**Details:**
  - **TestName:** test_init_with_large_split_number
  - **Description:** This test verifies that the `__init__` method correctly handles a very large `split_number`, ensuring that the split number is set correctly without causing any overflow or performance issues.

**Execution:**
  - **Arrange:** 
    - Create a temporary `.txt` file for testing.
  - **Act:** 
    - Instantiate the `Split_Files` class with the `.txt` file and a very large `split_number` (e.g., 1,000,000).
  - **Assert:** 
    - Verify that `self.split` is set to the correct large integer value.

**Validation:**
  - This test ensures that the initialization logic correctly handles very large `split_number` values, which is important for ensuring that the function can handle a wide range of input values without encountering issues related to integer overflow or performance degradation.

#### Scenario 9: Initialization with a Non-Existent File
**Details:**
  - **TestName:** test_init_with_non_existent_file
  - **Description:** This test verifies that the `__init__` method correctly handles the scenario where the provided file does not exist, ensuring that the function raises an appropriate exception or error.

**Execution:**
  - **Arrange:** 
    - Do not create any file; use a non-existent file path.
  - **Act:** 
    - Attempt to instantiate the `Split_Files` class with the non-existent file and a valid `split_number`.
  - **Assert:** 
    - Verify that an appropriate exception or error is raised (e.g., `FileNotFoundError`).

**Validation:**
  - This test ensures that the initialization logic correctly handles the scenario where the provided file does not exist, which is important for providing clear feedback to the user and preventing the function from proceeding with invalid input.

#### Scenario 10: Initialization with a Directory as Input File
**Details:**
  - **TestName:** test_init_with_directory_as_file
  - **Description:** This test verifies that the `__init__` method correctly handles the scenario where the provided file path is actually a directory, ensuring that the function raises an appropriate exception or error.

**Execution:**
  - **Arrange:** 
    - Create a temporary directory and use its path as the input file.
  - **Act:** 
    - Attempt to instantiate the `Split_Files` class with the directory path and a valid `split_number`.
  - **Assert:** 
    - Verify that an appropriate exception or error is raised (e.g., `IsADirectoryError`).

**Validation:**
  - This test ensures that the initialization logic correctly handles the scenario where the provided file path is actually a directory, which is important for providing clear feedback to the user and preventing the function from proceeding with invalid input.

### Summary
These test scenarios cover a wide range of potential behaviors and edge cases for the `__init__` method, ensuring that it functions correctly under various conditions. By validating the business logic encapsulated by the function, these tests help ensure that the file-splitting operation proceeds smoothly and that the function provides clear feedback in cases of invalid input.
"""

# ********RoostGPT********
import sys
import os
import shutil
import pytest
from split_files import Split_Files

class Test_SplitFilesInit:
    @pytest.mark.smoke
    @pytest.mark.valid
    def test_init_with_txt_file(self, tmpdir):
        # Arrange
        file_path = tmpdir.join("test_file.txt")
        file_path.write("Sample content")
        split_number = 5

        # Act
        split_files = Split_Files(str(file_path), split_number)

        # Assert
        assert split_files.file_extension == '.txt'
        assert os.path.exists(split_files.directory)
        assert len(os.listdir(split_files.directory)) == 0

    @pytest.mark.smoke
    @pytest.mark.valid
    def test_init_with_csv_file(self, tmpdir):
        # Arrange
        file_path = tmpdir.join("test_file.csv")
        file_path.write("Sample content")
        split_number = 5

        # Act
        split_files = Split_Files(str(file_path), split_number)

        # Assert
        assert split_files.file_extension == '.csv'
        assert os.path.exists(split_files.directory)
        assert len(os.listdir(split_files.directory)) == 0

    @pytest.mark.regression
    @pytest.mark.valid
    def test_init_with_existing_directory(self, tmpdir):
        # Arrange
        file_path = tmpdir.join("test_file.txt")
        file_path.write("Sample content")
        split_number = 5
        os.mkdir("file_split")
        with open("file_split/dummy.txt", "w") as f:
            f.write("Dummy content")

        # Act
        split_files = Split_Files(str(file_path), split_number)

        # Assert
        assert os.path.exists(split_files.directory)
        assert len(os.listdir(split_files.directory)) == 0

    @pytest.mark.regression
    @pytest.mark.invalid
    def test_init_with_invalid_file_extension(self, tmpdir):
        # Arrange
        file_path = tmpdir.join("test_file.docx")
        file_path.write("Sample content")
        split_number = 5

        # Act
        split_files = Split_Files(str(file_path), split_number)

        # Assert
        assert split_files.file_extension == '.csv'

    @pytest.mark.regression
    @pytest.mark.invalid
    def test_init_with_non_integer_split_number(self, tmpdir):
        # Arrange
        file_path = tmpdir.join("test_file.txt")
        file_path.write("Sample content")
        split_number = "5"

        # Act
        split_files = Split_Files(str(file_path), split_number)

        # Assert
        assert isinstance(split_files.split, int)

    @pytest.mark.regression
    @pytest.mark.invalid
    def test_init_with_negative_split_number(self, tmpdir):
        # Arrange
        file_path = tmpdir.join("test_file.txt")
        file_path.write("Sample content")
        split_number = -5

        # Act
        split_files = Split_Files(str(file_path), split_number)

        # Assert
        assert split_files.split > 0

    @pytest.mark.regression
    @pytest.mark.invalid
    def test_init_with_zero_split_number(self, tmpdir):
        # Arrange
        file_path = tmpdir.join("test_file.txt")
        file_path.write("Sample content")
        split_number = 0

        # Act
        split_files = Split_Files(str(file_path), split_number)

        # Assert
        assert split_files.split > 0

    @pytest.mark.performance
    @pytest.mark.valid
    def test_init_with_large_split_number(self, tmpdir):
        # Arrange
        file_path = tmpdir.join("test_file.txt")
        file_path.write("Sample content")
        split_number = 1000000

        # Act
        split_files = Split_Files(str(file_path), split_number)

        # Assert
        assert split_files.split == 1000000

    @pytest.mark.negative
    @pytest.mark.invalid
    def test_init_with_non_existent_file(self):
        # Arrange
        non_existent_file = "non_existent_file.txt"
        split_number = 5

        # Act & Assert
        with pytest.raises(FileNotFoundError):
            Split_Files(non_existent_file, split_number)

    @pytest.mark.negative
    @pytest.mark.invalid
    def test_init_with_directory_as_file(self, tmpdir):
        # Arrange
        dir_path = tmpdir.mkdir("test_dir")
        split_number = 5

        # Act & Assert
        with pytest.raises(IsADirectoryError):
            Split_Files(str(dir_path), split_number)
