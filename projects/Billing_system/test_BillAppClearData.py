# ********RoostGPT********
"""
Test generated by RoostGPT for test abc using AI Type Claude AI and AI Model claude-3-5-sonnet-20240620

ROOST_METHOD_HASH=clear_data_039b9b5ff8
ROOST_METHOD_SIG_HASH=clear_data_75709990c7


Here are the test scenarios for the clear_data method:

```
Scenario 1: Clear All Data Fields
Details:
  TestName: test_clear_all_data_fields
  Description: Verify that all data fields are reset to their initial values when the clear operation is confirmed.
Execution:
  Arrange: Initialize a Bill object with non-zero values for all fields.
  Act: Call the clear_data method and simulate user confirming the clear operation.
  Assert: Check that all IntVar and StringVar fields are reset to their default values (0 or "").
Validation:
  This test ensures the core functionality of the clear_data method, which is to reset all input fields and calculated values.

Scenario 2: Cancel Clear Operation
Details:
  TestName: test_cancel_clear_operation
  Description: Verify that no data is cleared when the user cancels the clear operation.
Execution:
  Arrange: Initialize a Bill object with non-zero values for all fields.
  Act: Call the clear_data method and simulate user canceling the clear operation.
  Assert: Verify that all field values remain unchanged.
Validation:
  This test confirms that the method respects the user's decision to cancel the clear operation, maintaining data integrity.

Scenario 3: Bill Number Regeneration
Details:
  TestName: test_bill_number_regeneration
  Description: Ensure a new random bill number is generated after clearing the data.
Execution:
  Arrange: Initialize a Bill object and store the initial bill number.
  Act: Call the clear_data method and simulate user confirming the clear operation.
  Assert: Verify that the bill_no field contains a new, different 4-digit number.
Validation:
  This test checks the proper functioning of the random bill number generation feature, which is crucial for unique bill identification.

Scenario 4: Welcome Bill Reset
Details:
  TestName: test_welcome_bill_reset
  Description: Verify that the welcome bill is reset to its initial state after clearing data.
Execution:
  Arrange: Initialize a Bill object and modify the bill area with customer data.
  Act: Call the clear_data method and simulate user confirming the clear operation.
  Assert: Check that the txtarea contains only the default welcome message and a new bill number.
Validation:
  This test ensures that the bill area is properly reset, which is important for starting a new bill entry.

Scenario 5: Clear Medical Items
Details:
  TestName: test_clear_medical_items
  Description: Verify that all medical item quantities are reset to zero.
Execution:
  Arrange: Initialize a Bill object with non-zero values for medical items.
  Act: Call the clear_data method and simulate user confirming the clear operation.
  Assert: Check that sanitizer, mask, hand_gloves, dettol, newsprin, and thermal_gun are all set to 0.
Validation:
  This test focuses on the correct resetting of medical item quantities, which is essential for accurate new bill creation.

Scenario 6: Clear Grocery Items
Details:
  TestName: test_clear_grocery_items
  Description: Verify that all grocery item quantities are reset to zero.
Execution:
  Arrange: Initialize a Bill object with non-zero values for grocery items.
  Act: Call the clear_data method and simulate user confirming the clear operation.
  Assert: Check that rice, food_oil, wheat, daal, flour, and maggi are all set to 0.
Validation:
  This test ensures the proper resetting of grocery item quantities, crucial for starting a new bill accurately.

Scenario 7: Clear Cold Drinks Items
Details:
  TestName: test_clear_cold_drinks_items
  Description: Verify that all cold drinks item quantities are reset to zero.
Execution:
  Arrange: Initialize a Bill object with non-zero values for cold drinks items.
  Act: Call the clear_data method and simulate user confirming the clear operation.
  Assert: Check that sprite, limka, mazza, coke, fanta, and mountain_duo are all set to 0.
Validation:
  This test confirms the correct resetting of cold drinks item quantities, essential for accurate new bill creation.

Scenario 8: Clear Price and Tax Fields
Details:
  TestName: test_clear_price_and_tax_fields
  Description: Verify that all price and tax fields are cleared.
Execution:
  Arrange: Initialize a Bill object with non-empty values for price and tax fields.
  Act: Call the clear_data method and simulate user confirming the clear operation.
  Assert: Check that medical_price, grocery_price, cold_drinks_price, medical_tax, grocery_tax, and cold_drinks_tax are all set to empty strings.
Validation:
  This test ensures that calculated prices and taxes are reset, which is crucial for starting a new bill calculation.

Scenario 9: Clear Customer Information
Details:
  TestName: test_clear_customer_information
  Description: Verify that customer name and phone number fields are cleared.
Execution:
  Arrange: Initialize a Bill object with non-empty values for customer name and phone number.
  Act: Call the clear_data method and simulate user confirming the clear operation.
  Assert: Check that c_name and c_phone are set to empty strings.
Validation:
  This test confirms that customer information is properly cleared, which is important for data privacy and preparing for a new customer entry.

Scenario 10: Preserve Search Bill Field
Details:
  TestName: test_preserve_search_bill_field
  Description: Verify that the search_bill field is cleared after the clear operation.
Execution:
  Arrange: Initialize a Bill object with a non-empty value in the search_bill field.
  Act: Call the clear_data method and simulate user confirming the clear operation.
  Assert: Check that the search_bill field is set to an empty string.
Validation:
  This test ensures that the search functionality is reset, allowing for new bill searches without interference from previous entries.
```

These scenarios cover the main aspects of the clear_data method, including its effect on various data fields, user interaction, and specific functionalities like bill number regeneration and welcome bill reset.
"""

# ********RoostGPT********
import pytest
from tkinter import *
from tkinter import messagebox
from Billing_system.biling_system import Bill_App

class TestBillAppClearData:

    @pytest.fixture
    def bill_app(self, monkeypatch):
        root = Tk()
        app = Bill_App(root)
        
        # Mock messagebox.askyesno to always return True
        monkeypatch.setattr(messagebox, "askyesno", lambda *args, **kwargs: True)
        
        # Mock random.randint to always return 1234
        monkeypatch.setattr("random.randint", lambda *args: 1234)
        
        return app

    def test_clear_all_data_fields(self, bill_app):
        # Set initial non-zero values
        bill_app.sanitizer.set(5)
        bill_app.rice.set(2)
        bill_app.sprite.set(3)
        bill_app.medical_price.set("100")
        bill_app.c_name.set("John Doe")
        bill_app.bill_no.set("5678")

        bill_app.clear_data()

        # Assert all fields are reset
        assert bill_app.sanitizer.get() == 0
        assert bill_app.rice.get() == 0
        assert bill_app.sprite.get() == 0
        assert bill_app.medical_price.get() == ""
        assert bill_app.c_name.get() == ""
        assert bill_app.bill_no.get() == "1234"

    def test_bill_number_regeneration(self, bill_app):
        initial_bill_no = bill_app.bill_no.get()
        bill_app.clear_data()
        assert bill_app.bill_no.get() != initial_bill_no
        assert bill_app.bill_no.get() == "1234"

    def test_welcome_bill_reset(self, bill_app, monkeypatch):
        # Mock welcome_bill method
        welcome_bill_called = False
        def mock_welcome_bill():
            nonlocal welcome_bill_called
            welcome_bill_called = True
        monkeypatch.setattr(bill_app, "welcome_bill", mock_welcome_bill)

        bill_app.clear_data()
        assert welcome_bill_called

    def test_clear_medical_items(self, bill_app):
        bill_app.sanitizer.set(5)
        bill_app.mask.set(3)
        bill_app.hand_gloves.set(2)
        bill_app.clear_data()
        assert bill_app.sanitizer.get() == 0
        assert bill_app.mask.get() == 0
        assert bill_app.hand_gloves.get() == 0

    def test_clear_grocery_items(self, bill_app):
        bill_app.rice.set(2)
        bill_app.food_oil.set(1)
        bill_app.wheat.set(3)
        bill_app.clear_data()
        assert bill_app.rice.get() == 0
        assert bill_app.food_oil.get() == 0
        assert bill_app.wheat.get() == 0

    def test_clear_cold_drinks_items(self, bill_app):
        bill_app.sprite.set(3)
        bill_app.limka.set(2)
        bill_app.mazza.set(1)
        bill_app.clear_data()
        assert bill_app.sprite.get() == 0
        assert bill_app.limka.get() == 0
        assert bill_app.mazza.get() == 0

    def test_clear_price_and_tax_fields(self, bill_app):
        bill_app.medical_price.set("100")
        bill_app.grocery_price.set("200")
        bill_app.cold_drinks_price.set("150")
        bill_app.medical_tax.set("10")
        bill_app.grocery_tax.set("20")
        bill_app.cold_drinks_tax.set("15")
        bill_app.clear_data()
        assert bill_app.medical_price.get() == ""
        assert bill_app.grocery_price.get() == ""
        assert bill_app.cold_drinks_price.get() == ""
        assert bill_app.medical_tax.get() == ""
        assert bill_app.grocery_tax.get() == ""
        assert bill_app.cold_drinks_tax.get() == ""

    def test_clear_customer_information(self, bill_app):
        bill_app.c_name.set("John Doe")
        bill_app.c_phone.set("1234567890")
        bill_app.clear_data()
        assert bill_app.c_name.get() == ""
        assert bill_app.c_phone.get() == ""

    def test_clear_search_bill_field(self, bill_app):
        bill_app.search_bill.set("5678")
        bill_app.clear_data()
        assert bill_app.search_bill.get() == ""

    def test_cancel_clear_operation(self, bill_app, monkeypatch):
        # Mock messagebox.askyesno to return False (simulating cancel)
        monkeypatch.setattr(messagebox, "askyesno", lambda *args, **kwargs: False)
        
        # Set some initial values
        bill_app.sanitizer.set(5)
        bill_app.c_name.set("John Doe")
        
        bill_app.clear_data()
        
        # Assert values remain unchanged
        assert bill_app.sanitizer.get() == 5
        assert bill_app.c_name.get() == "John Doe"

# TODO: Add more test cases if needed to cover additional scenarios

